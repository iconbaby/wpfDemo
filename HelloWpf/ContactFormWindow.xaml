<Window x:Class="HelloWpf.ContactFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloWpf"
        mc:Ignorable="d"
        Title="ContactFormWindow" Height="450" Width="450">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Margin="4,4,10,4">Name:</Label>
        <TextBox Grid.Column="1" Margin="4,4,30,4"></TextBox>
        <Label Grid.Row="1" Margin="4,4,10,4">E-mail:</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="4,4,30,4"></TextBox>
        <Label Grid.Row="2" Grid.Column="0" Margin="4,4,10,4">Comment:</Label>
        <TextBox Grid.Row="2" Grid.Column="2" AcceptsReturn="True" Margin="4,4,30,4"></TextBox>
    </Grid>
</Window>
